const getMessageNotification = state => state.notifications?.message;
const getVariantNotification = state => state.notifications?.variant;

export default { getMessageNotification, getVariantNotification };
